def general_poly(L):
    length = len(L)-1
    sum = 0
    return(sum)
    ''' 
L = [1, 2, 3, 4]
general_poly(L)
def general_poly(L):
    length = len(L)-1
    sum = 0
    for i in range(len(L)-1):
        sum += (L[i]**(length-=1))
    return(sum)
    ''' 
L = [1, 2, 3, 4]
general_poly(L)
